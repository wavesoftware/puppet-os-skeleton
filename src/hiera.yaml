---
# Used backends
:backends:
  - yaml

# Hierarchy - most important are on top (overwrites those bellow)
:hierarchy:
  - "nodes/%{hostname}"
  - "types/%{guess_type}"
  - common

# Yaml backend configuration
:yaml:
   :datadir: /etc/puppet/environments/%{environment}/hiera